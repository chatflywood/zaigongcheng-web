[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short

# pandas 2.2 announces an optional dependency for a future major release.
# Keep all other warnings visible.
filterwarnings =
    ignore:\s*Pyarrow will become a required dependency:DeprecationWarning
