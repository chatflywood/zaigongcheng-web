[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
# 禁用代理
env =
    ALL_PROXY=
    all_proxy=
    http_proxy=
    https_proxy=
    HTTP_PROXY=
    HTTPS_PROXY=
